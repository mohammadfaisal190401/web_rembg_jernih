services:
  - type: web
    name: web-rembg-jernih
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    
    gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0